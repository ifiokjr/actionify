# This file was autogenerated with actionify@0.2.0
# To update run: 
# deno run -Ar https://deno.land/x/actionify@0.2.0/cli.ts

name: ci
'on':
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  test:
    runs-on: ${{ matrix.os }}
    timeout-minutes: 5
    strategy:
      matrix:
        deno:
          - v1.24.x
          - v1.x
          - canary
        os:
          - macos-latest
          - ubuntu-latest
    steps:
      - name: Set environment
        run: echo "DENO_DIR=${{ runner.temp }}/deno_cache" >> $GITHUB_ENV
      - name: 🏴‍☠️ Checkout
        uses: actions/checkout@v3
      - name: 📦 Cache
        uses: actions/cache@v3
        with:
          path: ${{ env.DENO_DIR }}
          key: ${{ hashFiles('lock.json') }}-${{ matrix.deno }}-${{ matrix.os }}
      - uses: denoland/setup-deno@v1
        with:
          deno-version: ${{ matrix.deno }}
      - name: 🔒 Lock
        run: deno task lock
        shell: bash
        continue-on-error: true
      - if: ${{ matrix.os == 'ubuntu-latest' }}
        name: 🩺 Format
        uses: dprint/check@v2.0
      - if: ${{ matrix.os == 'ubuntu-latest' }}
        name: 👩‍⚕️ Lint
        run: deno lint
      - if: ${{ matrix.os == 'ubuntu-latest' }}
        name: 🩺 Typecheck
        run: deno task typecheck
      - name: ✅ Unittest
        run: deno task test
      - if: ${{ matrix.os == 'ubuntu-latest' }}
        name: 📝 Docs
        run: deno task test:docs
  publish:
    needs: test
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Set environment
        run: echo "DENO_DIR=${{ runner.temp }}/deno_cache" >> $GITHUB_ENV
      - name: 🏴‍☠️ Checkout
        uses: actions/checkout@v3
      - name: 📦 Cache
        uses: actions/cache@v3
        with:
          path: ${{ env.DENO_DIR }}
          key: ${{ hashFiles('lock.json') }}-v1.x-ubuntu-latest
      - uses: denoland/setup-deno@v1
        with:
          deno-version: v1.x
      - name: 🔒 Lock
        run: deno task lock
        shell: bash
        continue-on-error: true
      - name: 🦕 Publish
        run: deno task publish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
